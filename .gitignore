# IDE 工作区（记忆 + 临时工具），不进入版本库
.workbuddy/
.trae/

# 系统文件
Thumbs.db
.DS_Store
desktop.ini

# 构建产物（当前项目纯静态，无构建）
dist/
build/
node_modules/

# Python 缓存
__pycache__/
*.pyc

# 热榜原始快照（每轮热度值都会变），纯中间产物：
# 由 fetch.py 每轮重新生成，前端不读它，入库只会让每小时一次的调度刷出大量无意义提交
data/raw_hotspots.json
data/douyin_raw_hotspots.json

# 情感模型权重（约 390MB，不入库）
# —— 本仓库即 Pages 站点，模型入库会触发 GitHub 100MiB 单文件硬限制并被拒收
# 运行时由 scripts/sentiment.py 从 hf-mirror.com 下载并缓存到 models/hf-cache/
# 该目录由 GitHub Actions 的 actions/cache 持久化，缓存命中时无需重新下载
models/
